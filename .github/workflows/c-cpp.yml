name: C/C++ CI

on:
  push:
    branches: [ beta-prod ]
  pull_request:
    branches: [ beta-prod ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: make
      run: make
    - name: make check
      run: make check
