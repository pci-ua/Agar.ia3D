name: Build Windows

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  build:

    runs-on: windows-latest

    steps:
    - run: echo $SHELL
      shell: cmd
    - uses: actions/checkout@v2
    - name: Set up MinGW & GCC
      uses: egor-tensin/setup-mingw@v2
      with:
        platform: x64
    - run: echo $SHELL
      shell: cmd
    - name: make
      shell: cmd
      run: make
    - name: make check
      shell: cmd
      run: make check
