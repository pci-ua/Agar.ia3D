name: Build Windows

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  build:

    runs-on: windows-latest

    steps:
    - uses: actions/checkout@v2
    - name: Set up MinGW
      uses: egor-tensin/setup-mingw
      with:
        platform: x64
    - name: Set up GCC
      uses: egor-tensin/setup-gcc
      with:
        platform: x64
    - name: make
      run: make
    - name: make check
      run: make check
